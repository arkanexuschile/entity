/**
 * Tablas de infraestructura del Main — usuarios, roles, jobs.
 * Este archivo se expande en la Fase E (auth) y Fase G (scheduler).
 */

// Placeholder — se agregan tablas en las fases siguientes:
// - users (Fase E: autenticación)
// - roles (Fase E: permisos por módulo)
// - jobs (Fase G: scheduler)
