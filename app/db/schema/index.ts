/**
 * Barrel del esquema — re-exporta todos los módulos.
 * Drizzle lee este archivo como punto de entrada del esquema completo.
 *
 * IMPORTANTE: cada módulo edita solo su archivo y agrega su re-export acá.
 * No editar este archivo directamente salvo para agregar una línea de re-export.
 */

export * from "./main";
export * from "./finanzas";
export * from "./abc";
